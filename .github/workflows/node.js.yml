name: Node.js CI

on:
  push:
    branches: ["main"]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

<<<<<<< HEAD
      - name: Install dependencies
=======
      # 🚨 WAJIB DI FRONTEND
      - name: Install frontend dependencies
>>>>>>> 44750ec (fix CI: correct working directory for frontend)
        working-directory: frontend
        run: npm install

      - name: Build frontend
        working-directory: frontend
        run: npm run build
